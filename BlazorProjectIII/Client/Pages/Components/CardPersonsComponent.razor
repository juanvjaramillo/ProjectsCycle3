<section>
    <div class="jumbotron">
        <di v class="container-fluid">
            <div class="row mb-3">
                @foreach (var person in Persons) 
                {
                    <div class = "col-md-4">
                        <div class="card flex-md-row mb-4 box-shadow h-md-300">
                            <div class="card-body d-flex flex-column align-items-start">
                                <div class="card">
                                    <img src="@person.Photo" class="img-thumbnaill card-img-top">
                                    <div class="card-body">
                                        <h6 class="card-title">@person.PersonName</h6>
                                        <p class="text-muted">
                                            @person.DocumentType <br>
                                            @person.Document <br>
                                            <small>@person.Gender</small> <br>
                                            <small>@person.BirthDate</small>
                                        </p>
                                    </div>
                                    <div>
                                        <a href="http://"><span class="oi oi-pencil" aria-hidden="true"></span></a>
                                        <a href="http://"><span class="oi oi-zoom-in" aria-hidden="true"></span></a>
                                        <a href="http://"><span class="oi oi-delete" aria-hidden="true"></span></a>
                                        <button @onclick="MovieDelete">Eliminar</button>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                }   
            </div>
        </di>
    }
    </div>
</section>

@code{
    [Parameter] public List<Person> Persons{get; set;}

    void MovieDelete(){
        Console.WriteLine($"Eliminando el cartel de la película");
    }
}
