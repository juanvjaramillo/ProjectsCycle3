<section>
    <div class="jumbotron">
        <di v class="container-fluid">
            <div class="row mb-3">
                @foreach (var movie in Movies) 
                {
                    <div class = "col-md-4">
                        <div class="card flex-md-row mb-4 box-shadow h-md-300">
                            <div class="card-body d-flex flex-column align-items-start">
                                <div class="card">
                                    <img src="@movie.UriImage" class="img-thumbnaill card-img-top">
                                    <div class="card-body">
                                        <h6 class="card-title">@movie.Title</h6>
                                        <p class="text-muted">
                                            @movie.Synopsis <br>
                                            @movie.Genre <br>
                                            <small>@movie.Cast</small> <br>
                                            <small>@movie.ReleaseDate</small>
                                        </p>
                                    </div>
                                    <div>
                                        <button @onclick="MovieDelete">Eliminar</button>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                }   
            </div>
        </di>
    }
    </div>
</section>

@code{
    [Parameter] public List<Movie> Movies {get; set;}

    void MovieDelete(){
        Console.WriteLine($"Eliminando el cartel de la película");
    }
}
